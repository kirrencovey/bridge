import React, { Component } from "react"

export default class AnimalCard extends Component {
    render() {
        return (
            <React.Fragment>
                One Animal
            </React.Fragment>
        )
    }
}