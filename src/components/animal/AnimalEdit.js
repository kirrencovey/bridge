import React, { Component } from "react"

export default class AnimalEdit extends Component {
    render() {
        return (
            <React.Fragment>
                Animal Form with existing values pre-filled, for editing
            </React.Fragment>
        )
    }
}