import React, { Component } from "react"

export default class AnimalForm extends Component {
    render() {
        return (
            <React.Fragment>
                New Animal Form
            </React.Fragment>
        )
    }
}