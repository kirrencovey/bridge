import React, { Component } from "react"

export default class SessionForm extends Component {
    render() {
        return (
            <React.Fragment>
                New Session Form
            </React.Fragment>
        )
    }
}