import React, { Component } from "react"

export default class SessionEdit extends Component {
    render() {
        return (
            <React.Fragment>
                Session Form with existing values pre-filled, for editing
            </React.Fragment>
        )
    }
}