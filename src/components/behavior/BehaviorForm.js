import React, { Component } from "react"

export default class BehaviorForm extends Component {
    render() {
        return (
            <React.Fragment>
                New Behavior Form
            </React.Fragment>
        )
    }
}