import React, { Component } from "react"

export default class BehaviorEdit extends Component {
    render() {
        return (
            <React.Fragment>
                Behaivor Form with existing values pre-filled, for editing
            </React.Fragment>
        )
    }
}