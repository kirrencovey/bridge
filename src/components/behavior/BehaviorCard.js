import React, { Component } from "react"

export default class BehaviorCard extends Component {
    render() {
        return (
            <React.Fragment>
                One Behavior
            </React.Fragment>
        )
    }
}